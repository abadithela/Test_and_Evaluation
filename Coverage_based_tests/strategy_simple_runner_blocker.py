class runner(object):
    """Mealy transducer.

    Internal states are integers, the current state
    is stored in the attribute "state".
    To take a transition, call method "move".

    The names of input variables are stored in the
    attribute "input_vars".

    Automatically generated by tulip.dumpsmach on 2020-03-20 20:42:15 UTC
    To learn more about TuLiP, visit http://tulip-control.org
    """
    def __init__(self):
        self.state = 5
        self.input_vars = ['Cr']

    def move(self, Cr):
        """Given inputs, take move and return outputs.

        @rtype: dict
        @return: dictionary with keys of the output variable names:
            ['Xr']
        """
        output = dict()
        if self.state == 0:
            if (Cr == 3):
                self.state = 1

                output["Xr"] = 4
            else:
                self._error(Cr)
        elif self.state == 1:
            if (Cr == 4):
                self.state = 2

                output["Xr"] = 5
            elif (Cr == 2):
                self.state = 3

                output["Xr"] = 5
            else:
                self._error(Cr)
        elif self.state == 2:
            if (Cr == 3):
                self.state = 4

                output["Xr"] = 5
            else:
                self._error(Cr)
        elif self.state == 3:
            if (Cr == 3):
                self.state = 4

                output["Xr"] = 5
            else:
                self._error(Cr)
        elif self.state == 4:
            if (Cr == 4):
                self.state = 2

                output["Xr"] = 5
            elif (Cr == 2):
                self.state = 3

                output["Xr"] = 5
            else:
                self._error(Cr)
        elif self.state == 5:
            if (Cr == 2):
                self.state = 0

                output["Xr"] = 1
            else:
                self._error(Cr)
        else:
            raise Exception("Unrecognized internal state: " + str(self.state))
        return output

    def _error(self, Cr):
        raise ValueError("Unrecognized input: " + (
            "Cr = {Cr}; ").format(
                Cr=Cr))
